'use client';

import styles from './Main.module.scss';

const Main = () => {
    return (
        <section id={styles.mainPortfolio}>
            <div className={styles.mainPortfolioContainer}></div>
        </section>
    );
};

export default Main;
